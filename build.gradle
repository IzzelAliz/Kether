allprojects {
    group 'io.izzel.kether'
    version '1.0.0'

    tasks.withType(Jar) {
        destinationDirectory = file("$rootDir/build/libs")
    }

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
}

task clean {
    fileTree("$rootDir/build").each { it.delete() }
}